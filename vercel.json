{
  "version": 2,
  "buildCommand": "cd relief-hub-frontend && npm install && npm run build",
  "outputDirectory": "relief-hub-frontend/build",
  "installCommand": "cd relief-hub-frontend && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}